{
  "name": "aerial-kit - Full Dev (ROS Humble)",
  "build": {
    "dockerfile": "../docker/Dockerfile.humble",
    "context": "..",
    "target": "base"
  },
  "workspaceFolder": "/workspace",
  "workspaceMount": "source=${localWorkspaceFolder},target=/workspace,type=bind,consistency=cached",
  "customizations": {
    "vscode": {
      "extensions": [
        "ms-vscode.cpptools",
        "ms-vscode.cmake-tools",
        "ms-python.python",
        "ms-python.vscode-pylance",
        "ms-azuretools.vscode-docker",
        "redhat.vscode-yaml",
        "streetsidesoftware.code-spell-checker"
      ],
      "settings": {
        "python.defaultInterpreterPath": "/workspace/sim_py/.venv/bin/python",
        "python.linting.enabled": true,
        "python.linting.flake8Enabled": true,
        "python.formatting.provider": "autopep8",
        "C_Cpp.default.configurationProvider": "ms-vscode.cmake-tools",
        "cmake.configureOnOpen": false
      }
    }
  },
  "postCreateCommand": "sudo chown -R $(whoami) /workspace && cd /workspace/ros2_ws && rosdep update && rosdep install --from-paths src --ignore-src -r -y && . /opt/ros/humble/setup.bash && colcon build --symlink-install && cd /workspace && ./scripts/setup_sim_py_venv.sh && ./sim_py/.venv/bin/python -m pip install -r tools/requirements.txt -r tools/requirements-gps-dashboard.txt",
  "remoteUser": "ros",
  "features": {},
  "mounts": [
    "source=/dev,target=/dev,type=bind,consistency=cached"
  ],
  "runArgs": [
    "--privileged",
    "--network=host"
  ]
}
